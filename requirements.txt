# ── 必需 ─────────────────────────────────────────────
flask>=2.2
pdfplumber>=0.11        # 版式文本(layout=True)与页面结构分析
pypdfium2>=4            # 高质量按需渲染扫描页
pillow>=9
openpyxl>=3.1
anthropic>=0.40         # Anthropic Messages 协议客户端（内网网关与官方端点通用）

# ── 强烈建议（扫描件质量）────────────────────────────
# rapidocr-onnxruntime>=1.3   # 本地OCR：给视觉模型提供文字辅助；
#                             # 网关无视觉能力时是扫描件的唯一文字来源

# ── 可选增强 ─────────────────────────────────────────
# json_repair>=0.25     # 更强的坏JSON修复（内置修复已覆盖常见情况）

# ── 仅自测需要 ───────────────────────────────────────
# reportlab>=4          # selftest.py 生成中文样例PDF用，生产不需要
